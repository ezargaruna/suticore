# SUTI.world language registry

The MVP has two reviewed public pages: Russian (`/`) and English (`/en/`). Other requested language codes remain registered in `manifest.yml` without machine-generated public translations.

`draft` and `human review required` are explicit non-canonical states. Yazekh is referenced only through its existing corpus in `protocols/yazekh.md`; no fictitious translation was created.
